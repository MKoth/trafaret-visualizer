# Trafaret Visualizer (prototype)

Minimal prototype: upload PNGs with transparency, detect contours, and preview extruded "thick paper" pieces in a 3D scene.

Run locally:

```bash
npm install
npm run dev
```

Open `http://localhost:5173` and upload a PNG.

Notes:
- This scaffold includes core components; nesting and PDF export will be added next.
